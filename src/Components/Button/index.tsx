import React from "react";
import { View, Text, Pressable } from "react-native";

// TODO: add button

const Button = () => {
  return (
    <Pressable>
      <Text></Text>
    </Pressable>
  );
};

export default Button;
